[lavinna]
# This is a sample profile configuration
# You should replace any placeholder values with your actual AWS credentials if needed
# Note: Using OIDC in GitHub Actions is the recommended secure approach for production,
# but a profile can be useful for local development

# If using IAM user credentials (not recommended for production but useful for testing):
# aws_access_key_id = YOUR_ACCESS_KEY
# aws_secret_access_key = YOUR_SECRET_KEY

# If assuming a role (better approach):
# role_arn = arn:aws:iam::ACCOUNT_ID:role/ROLE_NAME
# source_profile = default
# region = us-east-1

# If using AWS SSO:
# sso_start_url = https://ORGANIZATION.awsapps.com/start
# sso_region = us-east-1
# sso_account_id = ACCOUNT_ID
# sso_role_name = ROLE_NAME
